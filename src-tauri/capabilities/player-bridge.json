{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "player-bridge",
  "description": "Let the embedded music.apple.com player window report bridge status back to the app.",
  "windows": ["player"],
  "remote": {
    "urls": ["https://music.apple.com"]
  },
  "permissions": ["core:default", "allow-player-bridge-report"]
}
